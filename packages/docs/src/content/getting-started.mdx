---
title: Getting Started
slug: /getting-started
group: Getting Started
order: 1
description: Install Manti UI, import its CSS, and render a React component.
---

# Getting Started

## Install

<InstallTabs packages="@manti-ui/react @manti-ui/styles" />

`@manti-ui/react` includes tokens and behavior. `@manti-ui/styles` is a peer
dependency so your app controls when CSS loads.

## Import CSS

Import the stylesheet once at the app entry:

```ts
import '@manti-ui/styles/index.css';
```

For Tailwind v4, use the [Tailwind guide](/guides/tailwind) instead.
Existing applications with global resets should also read the
[integration guide](/guides/integration).

## Render a component

<Demo name="button/variants" center defaultCodeOpen />

## Set theme preferences

Put preferences on `<html>` or any container:

```html
<html data-theme="dark" data-motion="default" data-radius="default">
  …
</html>
```

- `data-theme`: `light` or `dark`; omit it to follow the OS.
- `data-motion`: `default`, `none`, or `full`.
- `data-radius`: `none`, `sharp`, `default`, `round`, or `pill`.

## Next

- [Browse components](/components)
- [Learn the token model](/foundations/tokens)
- [Customize with plain CSS](/guides/plain-css)
