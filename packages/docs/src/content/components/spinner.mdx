---
title: Spinner
slug: /components/spinner
group: Feedback
order: 46
description: A calm, continuous loading indicator that inherits currentColor, so it adapts inside buttons, cards, or beside text.
---

# Spinner

A calm, continuous loading indicator. It inherits `currentColor`, so it adapts to
any context — drop it inside a button, a card, or beside text.

<Demo name="spinner/basic" center />

## Sizes

`size` sets the diameter and the track thickness together, so the ring keeps its
proportions at every size.

<Demo name="spinner/basic" center />

## Props

<PropsTable component="spinner" />

## Anatomy

Target these public selectors to customize the spinner. Class names and the DOM
between parts are private.

<Anatomy component="spinner" />

## Component tokens

<TokenTable component="spinner" />
