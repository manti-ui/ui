---
title: Collapsible
slug: /components/collapsible
group: Data Display
order: 21
description: Collapsible shows and hides a single region from a trigger, controlled with open and onOpenChange; indicator accepts false or a React node to replace it.
---

# Collapsible

Pass the button content as `trigger` and the revealed region as `children`. Use
`open` with `onOpenChange` to control it.

The default indicator follows state. Pass `indicator={false}` to hide it or a
React node to replace it.

<Demo name="collapsible/basic" center />

## API

<PropsTable component="collapsible" />

## Styling

### Anatomy

<Anatomy component="collapsible" />

### Tokens

<TokenTable component="collapsible" />
