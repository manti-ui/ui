---
title: Switch
slug: /components/switch
group: Inputs
order: 12
description: An on and off control with form behavior and two sizes.
---

# Switch

Use `defaultChecked` for uncontrolled state or `checked` with
`onCheckedChange` for controlled state. The actual input uses `role="switch"`.
`children` renders a visible label; use `inputProps` for `aria-label` or
`aria-labelledby` when the label lives elsewhere.

<Demo name="switch/basic" center />

## Sizes

<Demo name="switch/sizes" />

## API

<PropsTable component="switch" />

## Styling

### Anatomy

<Anatomy component="switch" />

### Tokens

<TokenTable component="switch" />
