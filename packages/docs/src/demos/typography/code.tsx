import { Code } from '@manti-ui/react';

export default function CodeDemo() {
  return (
    <p style={{ margin: 'var(--manti-space-0)' }}>
      Import <Code>Button</Code> from <Code>@manti-ui/react</Code>, then use the{' '}
      <Code size="md">variant</Code> prop to choose its emphasis.
    </p>
  );
}
