---
title: QrCode
slug: /components/qr-code
group: Data Display
order: 40
description: A QR code backed by the Zag.js qr-code machine — pass a value to encode, set the pixel size, and center an optional overlay on top.
---

# QrCode

A QR code backed by the Zag.js qr-code machine. Pass the `value` to encode and a
pixel `size`; an optional `overlay` can sit centered on top.

<Demo name="qr-code/basic" center />

## Props

<PropsTable component="qr-code" />

## Anatomy

Target these public selectors to customize the code. Class names and the DOM
between parts are private.

<Anatomy component="qr-code" />

## Component tokens

<TokenTable component="qr-code" />
